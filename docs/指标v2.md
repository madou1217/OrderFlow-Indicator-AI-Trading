# 指标 v2：OI 与 Long-Short Ratios 接入设计

> 本文只讨论以下范围：
> - `orderflow-market-data-ingestor`
> - 数据库改造
> - `orderflow-indicator-engine`
> - 最终写入 `/data/systems/llm/temp_indicator/*.json` 的指标结构
>
> 本文**不讨论**：
> - LLM prompt
> - entry/core/filter
> - scan/management 如何消费这些指标

---

## 1. 目标与边界

本次新增两类真正来自外部新数据源的指标：

- `i25 open_interest`
- `i26 long_short_ratios`

目标不是让模型去猜几分钟后的涨跌，而是给上层完整提供“仓位结构”这一层信息，补齐当前系统只看价格、成交、订单流、盘口、funding、liquidation，但**看不到总持仓和多空拥挤**的问题。

本次设计的硬要求：

- 最终 `temp_indicator` 中必须出现：
  - `indicators.open_interest`
  - `indicators.long_short_ratios`
- 两个指标都必须能稳定提供这 5 个窗口：
  - `5m`
  - `15m`
  - `4h`
  - `1d`
  - `3d`
- 改造边界只限于：
  - `orderflow-market-data-ingestor`
  - PostgreSQL 表结构与写入链路
  - `orderflow-indicator-engine`

---

## 2. 当前系统缺的是什么

当前已有指标已经能看到：

- trade / footprint / cvd / whale
- orderbook_depth / bbo / ofi / obi
- funding / mark / index / premium
- liquidation / PVS / TPO / RVWAP / FVG / EMA / VPIN

但当前系统仍然看不到两类关键信息：

- **总持仓是否在继续堆积或卸掉**
  - 这对应 `Open Interest`
- **市场和 Top Trader 的拥挤方向**
  - 这对应 `Global/Top Long-Short Ratios`

所以这次的核心不是再加一个“更聪明的订单流指标”，而是补上衍生品盘面里最缺的一层：**仓位结构**。

---

## 3. 外部数据源设计

## 3.1 Binance 数据源

### Open Interest

- 当前 OI：
  - `GET /fapi/v1/openInterest`
- OI history：
  - `GET /futures/data/openInterestHist`

### Long-Short Ratios

- Global Long/Short Account Ratio：
  - `GET /futures/data/globalLongShortAccountRatio`
- Top Trader Long/Short Account Ratio：
  - `GET /futures/data/topLongShortAccountRatio`
- Top Trader Long/Short Position Ratio：
  - `GET /futures/data/topLongShortPositionRatio`

## 3.2 接入方式

这几类源在 Binance 当前公开文档中都是 **REST**，不是公开 market-data WS stream。

因此设计上应采用：

- `current OI`：每 `1m` 轮询一次
- `OI history / ratios`：统一按 `5m` 为规范时钟轮询

## 3.3 为什么规范时钟用 5m

因为这次的硬目标窗口是：

- `5m`
- `15m`
- `4h`
- `1d`
- `3d`

全部都可以由 `5m` 序列稳定滚动聚合：

- `5m = 1`
- `15m = 3`
- `4h = 48`
- `1d = 288`
- `3d = 864`

所以本次方案不使用多套原始 period 并行接入，而是统一：

- OI history：只拉 `period=5m`
- ratios：只拉 `period=5m`

这样数据库、回放、重算、窗口一致性都会简单很多。

---

## 4. orderflow-market-data-ingestor 改造设计

## 4.1 新增 REST client 方法

在 `market_data_ingestor` 的 Binance REST client 中新增：

- `fetch_open_interest(symbol)`
- `fetch_open_interest_hist(symbol, period=5m, start_time, end_time, limit)`
- `fetch_global_long_short_account_ratio(symbol, period=5m, ...)`
- `fetch_top_long_short_account_ratio(symbol, period=5m, ...)`
- `fetch_top_long_short_position_ratio(symbol, period=5m, ...)`

实现位置建议：

- `/data/systems/market_data_ingestor/src/exchange/binance/rest/client.rs`
- 配套新增 REST DTO 文件：
  - `open_interest.rs`
  - `open_interest_hist.rs`
  - `long_short_ratio.rs`

## 4.2 新增标准化消息类型

### A. current OI

消息类型：

- `msg_type = "md.open_interest_current"`

推荐 routing key：

- `md.futures.open_interest.current.{symbol_lower}`

标准化字段：

```json
{
  "stream_name": "fapi/v1/openInterest",
  "ts_recv": "...",
  "ts_effective": "...",
  "open_interest_contracts": 10659.509,
  "mark_price": 71816.1,
  "open_interest_value_usdt": 765394630.0,
  "payload_json": {}
}
```

说明：

- `ts_effective` 取 Binance 返回时间
- `open_interest_value_usdt = open_interest_contracts * mark_price`
- `mark_price` 优先直接取当前 premium index / mark price；如果 REST 响应本身不带，则用最近一次 `md.mark_price` 或即时拉取后的值补齐

### B. OI 5m history

消息类型：

- `msg_type = "md.open_interest_hist_5m"`

推荐 routing key：

- `md.futures.open_interest.5m.{symbol_lower}`

标准化字段：

```json
{
  "stream_name": "futures/data/openInterestHist",
  "ts_recv": "...",
  "ts_effective": "...",
  "period": "5m",
  "open_interest_contracts": 20403.637,
  "open_interest_value_usdt": 150570784.0781,
  "payload_json": {}
}
```

说明：

- `ts_effective` 使用 Binance 历史记录时间戳
- 该表本身就是 `5m` 规范序列，不再做二次分钟化

### C. Long-Short Ratios 5m

消息类型：

- `msg_type = "md.long_short_ratio_5m"`

推荐 routing key：

- `md.futures.long_short_ratio.global_account.5m.{symbol_lower}`
- `md.futures.long_short_ratio.top_account.5m.{symbol_lower}`
- `md.futures.long_short_ratio.top_position.5m.{symbol_lower}`

标准化字段：

```json
{
  "stream_name": "futures/data/globalLongShortAccountRatio",
  "ts_recv": "...",
  "ts_effective": "...",
  "period": "5m",
  "ratio_type": "global_account",
  "long_short_ratio": 1.9559,
  "long_account_ratio": 0.6617,
  "short_account_ratio": 0.3382,
  "payload_json": {}
}
```

其中：

- `ratio_type ∈ {"global_account","top_account","top_position"}`

## 4.3 live 轮询任务

新增 2 类后台循环：

### current OI loop

- 频率：每 `1m`
- 只拉：
  - `/fapi/v1/openInterest`
- 写入：
  - DB
  - MQ / outbox

### 5m structure loop

- 频率：每 `5m` 闭合后
- 拉：
  - `/futures/data/openInterestHist?period=5m`
  - `/futures/data/globalLongShortAccountRatio?period=5m`
  - `/futures/data/topLongShortAccountRatio?period=5m`
  - `/futures/data/topLongShortPositionRatio?period=5m`
- 写入：
  - DB
  - MQ / outbox

## 4.4 startup backfill

新增一组 backfill 任务，风格与当前 funding / premium bootstrap 一致：

- 启动时补齐最近 30 天：
  - OI 5m 历史
  - Global ratio 5m 历史
  - Top account ratio 5m 历史
  - Top position ratio 5m 历史

### backfill 原则

- 只补 `5m` 规范序列
- 不补 `15m / 4h / 1d / 3d`
  - 这些窗口全部由 indicator_engine 聚合

---

## 5. 数据库改造设计

本次数据库改造只分两层：

- `md.*`：保存 market_data_ingestor 写入的原始/规范化源数据
- `feat.*`：保存 indicator_engine 聚合后的窗口特征

最终指标快照仍然进入现有：

- `feat.indicator_snapshot`

不需要新增 LLM 专用表。

## 5.1 md 层新增表

## A. `md.open_interest_current_1m`

用途：

- 保存每分钟轮询一次的当前 OI
- 提供 current sidecar 与断点恢复诊断

建议字段：

- `ts_event timestamptz not null`
- `market cfg.market_type not null`
- `symbol text not null`
- `source_kind cfg.source_type not null`
- `stream_name text not null`
- `open_interest_contracts double precision not null`
- `mark_price double precision`
- `open_interest_value_usdt double precision`
- `payload_json jsonb not null`

建议唯一键：

- `(market, symbol, ts_event)`

建议索引：

- `(symbol, ts_event desc)`

说明：

- 这里不再额外增加 `(market, symbol, ts_event desc)` 之类的第二条次级索引。
- 原因是该表只承载 futures OI current，写入频率为 `1m`，热路径查询也主要是按 `symbol + 最近时间` 取 latest/current sidecar。
- 保留一条幂等唯一键 + 一条 symbol-first lookup 索引即可，避免无意义的写放大。

## B. `md.open_interest_hist_5m`

用途：

- 保存 Binance 官方 5m OI 历史规范序列
- 作为 indicator_engine 的主分析输入

建议字段：

- `ts_event timestamptz not null`
- `ts_bucket timestamptz not null`
- `market cfg.market_type not null`
- `symbol text not null`
- `source_kind cfg.source_type not null`
- `stream_name text not null`
- `open_interest_contracts double precision not null`
- `open_interest_value_usdt double precision not null`
- `payload_json jsonb not null`

建议唯一键：

- `(market, symbol, ts_bucket)`

建议索引：

- `(symbol, ts_bucket desc)`

说明：

- 不单独存 `period` 列，因为表名已经固定为 `_5m`，这类冗余列只会让索引更宽。
- 不再额外增加 `(symbol, ts_event, market)` 回放索引，因为这里设计上 `ts_event = ts_bucket`，而 `indicator_engine` 对这类表的回放查询应按 `symbol + market + ts_bucket range` 走，自然可命中唯一键。
- 若未来要接多周期 raw OI history，应新增新的 raw 表或统一 raw 表，而不是在 `_5m` 规范表里重复存 period。

## C. `md.long_short_ratio_5m`

用途：

- 保存三种 ratio 的统一 5m 序列

建议字段：

- `ts_event timestamptz not null`
- `ts_bucket timestamptz not null`
- `market cfg.market_type not null`
- `symbol text not null`
- `source_kind cfg.source_type not null`
- `stream_name text not null`
- `ratio_type text not null`
- `long_short_ratio double precision not null`
- `long_account_ratio double precision`
- `short_account_ratio double precision`
- `payload_json jsonb not null`

建议唯一键：

- `(market, symbol, ratio_type, ts_bucket)`

建议索引：

- `(symbol, ratio_type, ts_bucket desc)`

### 备注

这里坚持把三种 ratio 放一张表，而不是拆三张表，原因是：

- 写入逻辑统一
- replay SQL 更简单
- 后续加别的 ratio type 不需要继续扩表
- 同时避免在表名已经固定为 `_5m` 的情况下，再把 `period` 放进行存储和唯一键，徒增索引宽度

## 5.2 feat 层新增表

## A. `feat.open_interest_feature`

用途：

- 保存 indicator_engine 对 OI 计算后的窗口特征
- 便于 SQL 调试、回测、复核与报警

建议字段：

- `ts_bucket timestamptz not null`
- `bar_interval interval not null`
- `symbol text not null`
- `oi_latest_contracts double precision`
- `oi_latest_value_usdt double precision`
- `oi_start_value_usdt double precision`
- `oi_delta_abs double precision`
- `oi_delta_pct double precision`
- `oi_log_return double precision`
- `oi_zscore double precision`
- `oi_accel double precision`
- `price_start double precision`
- `price_end double precision`
- `price_delta_pct double precision`
- `price_oi_relation text`
- `calc_version text not null`
- `extra_json jsonb not null`

建议唯一键：

- `(venue, symbol, bar_interval, ts_bucket)`

建议索引：

- `(symbol, bar_interval, ts_bucket desc)`

说明：

- 这里沿用现有 `feat.funding_feature / feat.orderbook_feature` 的自然键模式，把 `venue` 保留在唯一键里，避免未来多 venue 扩展时需要重做主索引。
- 不增加额外 `latest_effective_ts` / `state` 类索引，因为当前没有明确读路径会按这些字段高频过滤。

## B. `feat.long_short_ratio_feature`

用途：

- 保存 ratio 聚合后的窗口特征

建议字段：

- `ts_bucket timestamptz not null`
- `bar_interval interval not null`
- `symbol text not null`
- `global_ratio_latest double precision`
- `top_account_ratio_latest double precision`
- `top_position_ratio_latest double precision`
- `global_ratio_log double precision`
- `top_account_ratio_log double precision`
- `top_position_ratio_log double precision`
- `global_ratio_change double precision`
- `top_account_ratio_change double precision`
- `top_position_ratio_change double precision`
- `account_crowding_gap double precision`
- `position_crowding_gap double precision`
- `crowding_stretch double precision`
- `crowding_zscore double precision`
- `crowding_state text`
- `calc_version text not null`
- `extra_json jsonb not null`

建议唯一键：

- `(venue, symbol, bar_interval, ts_bucket)`

建议索引：

- `(symbol, bar_interval, ts_bucket desc)`

说明：

- 和现有 `feat.*` 表保持相同的 hypertable + natural upsert 风格。
- 不为 `crowding_state`、`crowding_strength`、`extra_json` 额外建索引，避免为了低频调试查询牺牲写入成本。

## 5.3 indicator snapshot

不需要新增新表。

沿用现有：

- `feat.indicator_snapshot`

只需新增两个指标编码进入 snapshot：

- `i25 / open_interest`
- `i26 / long_short_ratios`

当前 snapshot 表按 `(ts_snapshot, symbol, indicator_code, window_code)` 幂等写入，完全适合本次扩展。[snapshot_writer.rs](/data/systems/indicator_engine/src/storage/snapshot_writer.rs#L70)

## 5.4 migration 建议

建议新增以下 migration：

- `20260326_md_open_interest_current_1m.sql`
- `20260326_md_open_interest_hist_5m.sql`
- `20260326_md_long_short_ratio_5m.sql`
- `20260326_feat_open_interest_feature.sql`
- `20260326_feat_long_short_ratio_feature.sql`
- `20260326_indicator_catalog_oi_ratio.sql`

---

## 6. orderflow-indicator-engine 改造设计

## 6.1 配置层改造

当前 `indicator.window_codes` 只接受：

- `1m`
- `15m`
- `1h`
- `4h`
- `1d`
- `3d`

代码和配置都还不接受 `5m`。[bootstrap.rs](/data/systems/indicator_engine/src/app/bootstrap.rs#L658) [bootstrap.rs](/data/systems/indicator_engine/src/app/bootstrap.rs#L1591) [config.yaml](/data/config/config.yaml#L63)

因此必须先做这一步：

- 在 `default_window_codes()` 中加入 `5m`
- 在配置校验中加入 `5m`
- 在 `config.yaml` 中把 `indicator.window_codes` 扩为：
  - `["1m", "5m", "15m", "1h", "4h", "1d", "3d"]`

说明：

- 这里的 `5m` 是为了让新指标 `i25 / i26` 能合法出现在统一窗口体系中
- 不是要求所有旧指标都必须补做 `5m`
- 旧指标若不支持 `5m`，可以继续维持现状；本次只要求 `i25 / i26` 的 `by_window` 必须覆盖 `5m / 15m / 4h / 1d / 3d`

## 6.2 ingest / replay 改造

indicator_engine 现在的 replay SQL 只 union：

- `md.agg.trade.1m`
- `md.agg.orderbook.1m`
- `md.agg.liq.1m`
- `md.agg.funding_mark.1m`

本次需要追加 3 类输入：

- `md.open_interest_current`
- `md.open_interest_hist_5m`
- `md.long_short_ratio_5m`

改造点：

- live MQ 消费：
  - 新增解码这 3 类 `msg_type`
- startup replay / canonical replay：
  - 从新建的 `md.*` 表把这 3 类数据 union 进事件流

### 设计原则

- `open_interest_hist_5m` 和 `long_short_ratio_5m` 是**窗口计算主输入**
- `open_interest_current` 只是 current sidecar，不参与 `15m/4h/1d/3d` 主窗口计算

## 6.3 runtime state 改造

在 runtime state 中新增 3 组缓冲：

### A. current OI slot

保存最近一个 current OI：

- `ts_effective`
- `open_interest_contracts`
- `mark_price`
- `open_interest_value_usdt`

### B. OI 5m deque

按时间排序保存最近足够覆盖 `3d` 的 5m 点：

- `ts_bucket`
- `open_interest_contracts`
- `open_interest_value_usdt`
- `reference_price`

最少保留：

- `864 + 安全余量`

### C. long-short ratio 5m deque

分别为三种 ratio_type 维护 5m 序列：

- `global_account`
- `top_account`
- `top_position`

每个点保存：

- `ts_bucket`
- `long_short_ratio`
- `long_account_ratio`
- `short_account_ratio`

## 6.4 新增指标实现

### `i25_open_interest.rs`

职责：

- 基于 `open_interest_hist_5m` 生成窗口：
  - `5m`
  - `15m`
  - `4h`
  - `1d`
  - `3d`
- 并把 `current OI` 补进顶层 sidecar

### `i26_long_short_ratios.rs`

职责：

- 基于三类 5m ratio 序列生成窗口：
  - `5m`
  - `15m`
  - `4h`
  - `1d`
  - `3d`

## 6.5 feature_writer 改造

在现有 `feature_writer` 的窗口写入路径中新增：

- `insert_open_interest_feature_window(...)`
- `insert_long_short_ratio_feature_window(...)`

风格应与现有 `insert_funding_feature_window(...)` 一致。[feature_writer.rs](/data/systems/indicator_engine/src/storage/feature_writer.rs#L560)

## 6.6 indicator catalog 改造

在现有 catalog 中新增：

- `("i25", "open_interest")`
- `("i26", "long_short_ratios")`

当前 catalog 定义在 [runtime.rs](/data/systems/indicator_engine/src/app/runtime.rs#L1067) 一带。

---

## 7. 指标口径设计

## 7.1 i25 open_interest

## 目标

为上层提供：

- 当前 OI 是否在上升/下降
- 价格与 OI 的组合关系
- 当前 move 更像：
  - `leveraged_long_build`
  - `fresh_short_build`
  - `long_unwind`
  - `short_cover`

## 规范输入

主输入一律使用 `5m` OI history：

- `OI_t = open_interest_value_usdt`
- `P_t = 同时刻参考价格`

参考价格优先级：

1. 同时刻 `mark_price`
2. 若缺失，则最近 futures mark price
3. 再缺失则使用 futures 价格 close

## 计算窗口

所有窗口都从 `5m` 序列聚合：

- `5m = 1`
- `15m = 3`
- `4h = 48`
- `1d = 288`
- `3d = 864`

## 核心字段

每个窗口至少输出：

- `window_minutes`
- `samples_used`
- `oi_start`
- `oi_end`
- `oi_delta_abs`
- `oi_delta_pct`
- `oi_log_return`
- `oi_zscore`
- `oi_accel`
- `price_start`
- `price_end`
- `price_delta_pct`
- `price_oi_relation`
- `state`

## 分类口径

### leveraged_long_build

- 价格涨
- OI 涨

### fresh_short_build

- 价格跌
- OI 涨

### long_unwind

- 价格跌
- OI 跌

### short_cover

- 价格涨
- OI 跌

### neutral

- 其余情况

## 7.2 i26 long_short_ratios

## 目标

为上层提供：

- 全市场是否拥挤多 / 拥挤空
- Top Trader 是否比全市场更偏一侧
- 账户比与持仓比是否同步变极端

## 规范输入

使用三条 `5m` 序列：

- `global_account`
- `top_account`
- `top_position`

## 核心字段

每个窗口至少输出：

- `global_ratio_latest`
- `top_account_ratio_latest`
- `top_position_ratio_latest`
- `global_ratio_log`
- `top_account_ratio_log`
- `top_position_ratio_log`
- `global_ratio_change`
- `top_account_ratio_change`
- `top_position_ratio_change`
- `account_crowding_gap`
- `position_crowding_gap`
- `crowding_stretch`
- `crowding_zscore`
- `crowding_state`

## crowding_state 口径

仅允许：

- `crowded_long`
- `crowded_short`
- `long_crowding_unwind`
- `short_crowding_unwind`
- `balanced`

---

## 8. temp_indicator 输出契约

本次最重要的是最终 `/data/systems/llm/temp_indicator/*.json` 的稳定结构。

## 8.1 open_interest

推荐结构：

```json
{
  "open_interest": {
    "window_code": "1m",
    "payload": {
      "as_of_ts": "...",
      "open_interest_contracts": 10659.509,
      "mark_price": 71816.1,
      "open_interest_value_usdt": 765394630.0,
      "delta_1m_abs": 123456.0,
      "delta_1m_pct": 0.0016,
      "delta_5m_abs": 928374.0,
      "delta_5m_pct": 0.0122,
      "state_1m": "leveraged_long_build",
      "by_window": {
        "5m": {
          "window_minutes": 5,
          "samples_used": 1,
          "oi_start": 764466256.0,
          "oi_end": 765394630.0,
          "oi_delta_abs": 928374.0,
          "oi_delta_pct": 0.001214,
          "oi_log_return": 0.001213,
          "oi_zscore": 1.42,
          "oi_accel": 0.0008,
          "price_start": 71740.5,
          "price_end": 71816.1,
          "price_delta_pct": 0.00105,
          "price_oi_relation": "leveraged_long_build",
          "state": "leveraged_long_build"
        },
        "15m": {},
        "4h": {},
        "1d": {},
        "3d": {}
      }
    }
  }
}
```

### 约束

- `payload.by_window` **必须且只允许**：
  - `5m`
  - `15m`
  - `4h`
  - `1d`
  - `3d`

## 8.2 long_short_ratios

推荐结构：

```json
{
  "long_short_ratios": {
    "window_code": "5m",
    "payload": {
      "as_of_ts": "...",
      "global_account_ratio": 1.21,
      "top_account_ratio": 1.58,
      "top_position_ratio": 1.83,
      "crowding_state_current": "crowded_long",
      "by_window": {
        "5m": {
          "global_ratio_latest": 1.21,
          "top_account_ratio_latest": 1.58,
          "top_position_ratio_latest": 1.83,
          "global_ratio_log": 0.1906,
          "top_account_ratio_log": 0.4574,
          "top_position_ratio_log": 0.6043,
          "global_ratio_change": 0.021,
          "top_account_ratio_change": 0.055,
          "top_position_ratio_change": 0.062,
          "account_crowding_gap": 0.2668,
          "position_crowding_gap": 0.4137,
          "crowding_stretch": 0.8711,
          "crowding_zscore": 1.94,
          "crowding_state": "crowded_long"
        },
        "15m": {},
        "4h": {},
        "1d": {},
        "3d": {}
      }
    }
  }
}
```

### 约束

- `payload.by_window` **必须且只允许**：
  - `5m`
  - `15m`
  - `4h`
  - `1d`
  - `3d`

---

## 9. 与现有存储结构的对齐点

### market_data_ingestor

当前已有 `md.mark_price_funding_1s`、`md.funding_rate_event`、`md.force_order_event`、`md.agg_*_1m` 这类风格。[md_db_writer.rs](/data/systems/market_data_ingestor/src/sinks/md_db_writer.rs#L606) [md_db_writer.rs](/data/systems/market_data_ingestor/src/sinks/md_db_writer.rs#L640) [md_db_writer.rs](/data/systems/market_data_ingestor/src/sinks/md_db_writer.rs#L671) [md_db_writer.rs](/data/systems/market_data_ingestor/src/sinks/md_db_writer.rs#L898)

本次新增表应继续沿用这个命名与 upsert 风格，不要单独发明一套不一致的 schema。

### indicator_engine

当前 feature_writer 也是按窗口写 `feat.*`，再统一进 `feat.indicator_snapshot`。[feature_writer.rs](/data/systems/indicator_engine/src/storage/feature_writer.rs#L67) [feature_writer.rs](/data/systems/indicator_engine/src/storage/feature_writer.rs#L560)

因此 OI 与 ratio 也应该：

- 先落 `feat.open_interest_feature`
- 再落 `feat.long_short_ratio_feature`
- 最后产出 `feat.indicator_snapshot`

---

## 10. 最小实施顺序

### Phase 1：ingestor 与 md 表

- REST client 接口
- live polling loop
- backfill loop
- `md.open_interest_current_1m`
- `md.open_interest_hist_5m`
- `md.long_short_ratio_5m`

### Phase 2：indicator_engine 与 feat 表

- 配置支持 `5m`
- ingest / replay 支持新 `md.*`
- runtime state 增加 5m OI / ratio 序列
- `feat.open_interest_feature`
- `feat.long_short_ratio_feature`

### Phase 3：snapshot / temp_indicator

- 注册 `i25 / i26`
- 产出 `feat.indicator_snapshot`
- 确认 `temp_indicator` 中出现：
  - `indicators.open_interest`
  - `indicators.long_short_ratios`
- 确认 `by_window` 恰好含：
  - `5m / 15m / 4h / 1d / 3d`

---

## 11. 需要新增/修改的 Rust 文件清单

## 11.1 orderflow-market-data-ingestor

### 新增文件

- `/data/systems/market_data_ingestor/src/exchange/binance/rest/open_interest.rs`
  - `GET /fapi/v1/openInterest` 的 DTO
- `/data/systems/market_data_ingestor/src/exchange/binance/rest/open_interest_hist.rs`
  - `GET /futures/data/openInterestHist` 的 DTO
- `/data/systems/market_data_ingestor/src/exchange/binance/rest/long_short_ratio.rs`
  - 三类 ratio 的统一 DTO
- `/data/systems/market_data_ingestor/src/normalize/open_interest_normalizer.rs`
  - `md.open_interest_current`
  - `md.open_interest_hist_5m`
- `/data/systems/market_data_ingestor/src/normalize/long_short_ratio_normalizer.rs`
  - `md.long_short_ratio_5m`

### 修改文件

- `/data/systems/market_data_ingestor/src/exchange/binance/rest/mod.rs`
  - 注册 3 个新 REST 模块
- `/data/systems/market_data_ingestor/src/exchange/binance/rest/client.rs`
  - 新增 5 个 REST fetch 方法
- `/data/systems/market_data_ingestor/src/normalize/mod.rs`
  - 注册两个新 normalizer
  - 若采用统一 helper，可补 `normalize_open_interest_rest(...)`
  - 若采用统一 helper，可补 `normalize_long_short_ratio_rest(...)`
- `/data/systems/market_data_ingestor/src/state/backfill_scheduler.rs`
  - 新增 OI / ratio live polling 与 startup backfill 调度
- `/data/systems/market_data_ingestor/src/app/runtime.rs`
  - 启动新的 scheduler/task
- `/data/systems/market_data_ingestor/src/sinks/md_db_writer.rs`
  - 新增 3 类 `md.*` 写入分支与 SQL
- `/data/systems/market_data_ingestor/src/sinks/mq_publisher.rs`
  - 允许这 3 类新消息直接发 MQ
- `/data/systems/market_data_ingestor/src/pipelines/futures_pipeline.rs`
  - reconnect/bootstrap 时补齐 OI / ratio 恢复逻辑
- `/data/systems/market_data_ingestor/src/observability/metrics.rs`
  - 新增 REST 拉取成功/失败指标

### 可选修改文件

- `/data/systems/market_data_ingestor/src/sinks/ops_db_writer.rs`
  - 记录 OI / ratio backfill job run
- `/data/systems/market_data_ingestor/src/observability/heartbeat.rs`
  - 暴露 OI / ratio 最新成功时间

## 11.2 orderflow-indicator-engine

### 新增文件

- `/data/systems/indicator_engine/src/indicators/i25_open_interest.rs`
- `/data/systems/indicator_engine/src/indicators/i26_long_short_ratios.rs`

### 修改文件

- `/data/systems/indicator_engine/src/indicators/mod.rs`
  - 注册 `i25` / `i26`
- `/data/systems/indicator_engine/src/indicators/registry.rs`
  - 加入 `I25OpenInterest`
  - 加入 `I26LongShortRatios`
- `/data/systems/indicator_engine/src/ingest/decoder.rs`
  - 解码：
    - `md.open_interest_current`
    - `md.open_interest_hist_5m`
    - `md.long_short_ratio_5m`
- `/data/systems/indicator_engine/src/runtime/state_store.rs`
  - 新增 OI current slot
  - 新增 OI 5m deque
  - 新增 ratio 5m deque
  - 新增对应 ingest/update/finalize 逻辑
- `/data/systems/indicator_engine/src/storage/feature_writer.rs`
  - 新增：
    - `insert_open_interest_feature_window(...)`
    - `insert_long_short_ratio_feature_window(...)`
- `/data/systems/indicator_engine/src/app/runtime.rs`
  - replay SQL union 新 `md.*`
  - catalog 加入 `i25` / `i26`
- `/data/systems/indicator_engine/src/app/bootstrap.rs`
  - `window_codes` 支持 `5m`
- `/data/systems/indicator_engine/src/indicators/context.rs`
  - 若窗口辅助函数不支持 `5m`，需要扩展

### 可选新增文件

- `/data/systems/indicator_engine/src/indicators/shared/open_interest.rs`
- `/data/systems/indicator_engine/src/indicators/shared/long_short_ratio.rs`

说明：

- 如果 `i25` / `i26` 的窗口统计逻辑较长，建议把公共计算抽到 `shared/`，避免两个指标文件臃肿。

## 11.3 现有文件为何要这么拆

这套文件清单遵循仓库当前模块组织：

- REST DTO 放在 `exchange/binance/rest/`
- 标准化逻辑放在 `normalize/`
- DB 写入放在 `sinks/md_db_writer.rs`
- 指标实现放在 `indicator_engine/src/indicators/`
- 窗口特征入库放在 `storage/feature_writer.rs`
- 最终快照仍由 `storage/snapshot_writer.rs` 统一写入

这样改动最符合当前代码库结构，不需要发明第二套组织方式。

---

## 12. 需要新增的 migration 草案清单

## 12.1 推荐 migration 文件名

- `/data/sql/migrations/20260326_md_open_interest_current_1m.sql`
- `/data/sql/migrations/20260326_md_open_interest_hist_5m.sql`
- `/data/sql/migrations/20260326_md_long_short_ratio_5m.sql`
- `/data/sql/migrations/20260326_feat_open_interest_feature.sql`
- `/data/sql/migrations/20260326_feat_long_short_ratio_feature.sql`
- `/data/sql/migrations/20260326_indicator_catalog_oi_ratio.sql`

## 12.2 每个 migration 的职责

### `20260326_md_open_interest_current_1m.sql`

- 创建 `md.open_interest_current_1m`
- 建主键/唯一键
- 建 `(symbol, ts_event desc)` 索引

### `20260326_md_open_interest_hist_5m.sql`

- 创建 `md.open_interest_hist_5m`
- 建 `(market, symbol, period, ts_bucket)` 唯一键
- 建 `(symbol, ts_bucket desc)` 索引

### `20260326_md_long_short_ratio_5m.sql`

- 创建 `md.long_short_ratio_5m`
- 建 `(market, symbol, period, ratio_type, ts_bucket)` 唯一键
- 建 `(symbol, ratio_type, ts_bucket desc)` 索引

### `20260326_feat_open_interest_feature.sql`

- 创建 `feat.open_interest_feature`
- 建 `(symbol, bar_interval, ts_bucket)` 唯一键
- 如需要，建 `(symbol, ts_bucket desc)` 辅助索引

### `20260326_feat_long_short_ratio_feature.sql`

- 创建 `feat.long_short_ratio_feature`
- 建 `(symbol, bar_interval, ts_bucket)` 唯一键
- 如需要，建 `(symbol, ts_bucket desc)` 辅助索引

### `20260326_indicator_catalog_oi_ratio.sql`

- 若项目里有 indicator catalog / metadata 表，则补：
  - `i25 open_interest`
  - `i26 long_short_ratios`
- 如果 catalog 目前是纯代码常量，这个 migration 可以不建表，只保留文档占位

## 12.3 migration 顺序要求

推荐顺序：

1. 先建 `md.*`
2. 再建 `feat.*`
3. 最后补 catalog / metadata

原因：

- ingestor 可以先落原始数据
- indicator_engine 随后再依赖这些 `md.*`
- catalog 属于最末层元数据

---

## 13. 每个服务的改造顺序清单

## 13.1 orderflow-market-data-ingestor 改造顺序

### Step 1：REST DTO 与 client

- 新增 DTO：
  - `open_interest.rs`
  - `open_interest_hist.rs`
  - `long_short_ratio.rs`
- 在 `client.rs` 中实现 fetch 方法

### Step 2：normalizer

- 新增：
  - `open_interest_normalizer.rs`
  - `long_short_ratio_normalizer.rs`
- 明确三种 `msg_type`
- 明确 routing key

### Step 3：md_db_writer

- 加入 3 类消息的 insert / upsert SQL
- 保证幂等键合理

### Step 4：scheduler / runtime

- 在 `backfill_scheduler.rs` 中：
  - 加 `current OI` 1m loop
  - 加 `5m OI / ratio` loop
  - 加 startup backfill
- 在 `app/runtime.rs` 中挂起这些任务

### Step 5：MQ / outbox

- 确认新消息能走：
  - direct publish
  - outbox fallback

### Step 6：验收

- DB 中出现：
  - `md.open_interest_current_1m`
  - `md.open_interest_hist_5m`
  - `md.long_short_ratio_5m`
- RabbitMQ 中出现对应 `md.*` 路由

## 13.2 orderflow-indicator-engine 改造顺序

### Step 1：配置支持 5m

- `bootstrap.rs`
- `config.yaml`

### Step 2：decoder / replay

- 解码 3 类新 `md.*`
- replay SQL union 3 类新 `md.*`

### Step 3：runtime state

- current OI slot
- OI 5m deque
- ratio 5m deque

### Step 4：新指标实现

- `i25_open_interest.rs`
- `i26_long_short_ratios.rs`

### Step 5：feature_writer

- 写：
  - `feat.open_interest_feature`
  - `feat.long_short_ratio_feature`

### Step 6：registry / catalog / snapshot

- 注册 `i25` / `i26`
- 进入 `feat.indicator_snapshot`

### Step 7：验收

- `feat.indicator_snapshot` 中看到：
  - `indicator_code = open_interest`
  - `indicator_code = long_short_ratios`
- `window_code` 必须覆盖：
  - `5m`
  - `15m`
  - `4h`
  - `1d`
  - `3d`
- `temp_indicator` 中看到：
  - `indicators.open_interest`
  - `indicators.long_short_ratios`

## 13.3 联调顺序

推荐联调顺序：

1. 先只做 ingestor，把 `md.*` 表和 MQ 路由打通
2. 再做 indicator_engine 的 ingest / replay
3. 再做 `i25 / i26`
4. 最后检查 `temp_indicator` 的最终 JSON 契约

这样可以把问题快速切分为：

- 是源没进来
- 还是 DB 没落
- 还是 replay 没吃到
- 还是指标没算出来
- 还是 snapshot/temp_indicator 没映射出来

---

## 14. 逐文件字段设计

## 14.1 orderflow-market-data-ingestor

### `/data/systems/market_data_ingestor/src/exchange/binance/rest/open_interest.rs`

建议只放 DTO，不做业务解释：

```rust
use serde::Deserialize;

#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct BinanceOpenInterestResponse {
    pub symbol: String,
    pub open_interest: String,
    pub time: i64,
}
```

设计要点：

- `open_interest` 保持 `String`，延续当前 REST DTO 风格，在 normalizer 层再做数值解析。
- 这里不放 `mark_price`，因为 Binance 这个接口本身不返回。

### `/data/systems/market_data_ingestor/src/exchange/binance/rest/open_interest_hist.rs`

```rust
use serde::Deserialize;

#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct BinanceOpenInterestHistRecord {
    pub symbol: String,
    pub sum_open_interest: String,
    pub sum_open_interest_value: String,
    pub timestamp: i64,
}
```

设计要点：

- `sum_open_interest` 和 `sum_open_interest_value` 保留 Binance 原始口径。
- `timestamp` 对应 `5m` bucket 的有效时间戳，后续直接映射到 `ts_bucket` / `ts_effective`。

### `/data/systems/market_data_ingestor/src/exchange/binance/rest/long_short_ratio.rs`

建议统一三个 ratio endpoint 的 DTO，并显式定义 ratio 类型：

```rust
use serde::Deserialize;

#[derive(Debug, Clone, Copy)]
pub enum LongShortRatioType {
    GlobalAccount,
    TopAccount,
    TopPosition,
}

impl LongShortRatioType {
    pub fn as_str(self) -> &'static str {
        match self {
            Self::GlobalAccount => "global_account",
            Self::TopAccount => "top_account",
            Self::TopPosition => "top_position",
        }
    }
}

#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct BinanceLongShortRatioRecord {
    pub symbol: String,
    pub long_short_ratio: String,
    pub long_account: String,
    pub short_account: String,
    pub timestamp: i64,
}
```

设计要点：

- 三个 endpoint 的响应结构相同，差异由调用方额外附带 `ratio_type`。
- `long_account` / `short_account` 保持原始字符串，normalizer 再转成 `f64`。

### `/data/systems/market_data_ingestor/src/exchange/binance/rest/client.rs`

建议新增以下方法：

```rust
pub async fn fetch_open_interest(
    &self,
    symbol: &str,
) -> Result<BinanceOpenInterestResponse>;

pub async fn fetch_open_interest_hist(
    &self,
    symbol: &str,
    period: &str,
    start_time_ms: Option<i64>,
    end_time_ms: Option<i64>,
    limit: usize,
) -> Result<Vec<BinanceOpenInterestHistRecord>>;

pub async fn fetch_global_long_short_account_ratio(
    &self,
    symbol: &str,
    period: &str,
    start_time_ms: Option<i64>,
    end_time_ms: Option<i64>,
    limit: usize,
) -> Result<Vec<BinanceLongShortRatioRecord>>;

pub async fn fetch_top_long_short_account_ratio(
    &self,
    symbol: &str,
    period: &str,
    start_time_ms: Option<i64>,
    end_time_ms: Option<i64>,
    limit: usize,
) -> Result<Vec<BinanceLongShortRatioRecord>>;

pub async fn fetch_top_long_short_position_ratio(
    &self,
    symbol: &str,
    period: &str,
    start_time_ms: Option<i64>,
    end_time_ms: Option<i64>,
    limit: usize,
) -> Result<Vec<BinanceLongShortRatioRecord>>;
```

实现约束：

- `period` 在调用端固定传 `5m`，不要在多个地方分叉。
- startup backfill 必须支持 `start_time_ms / end_time_ms / limit`。
- live loop 建议每次重拉最近 `2` 个 `5m` bucket，利用 DB upsert 保持幂等。

### `/data/systems/market_data_ingestor/src/normalize/open_interest_normalizer.rs`

建议提供两个入口：

```rust
pub fn normalize_open_interest_current_rest(
    symbol: &str,
    row: &BinanceOpenInterestResponse,
    mark_price: Option<f64>,
    backfill_in_progress: bool,
) -> Result<NormalizedMdEvent>;

pub fn normalize_open_interest_hist_rest(
    symbol: &str,
    row: &BinanceOpenInterestHistRecord,
    backfill_in_progress: bool,
) -> Result<NormalizedMdEvent>;
```

其中 `data` 字段建议固定为：

```json
{
  "stream_name": "fapi/v1/openInterest",
  "ts_recv": "2026-03-26T00:00:00Z",
  "ts_effective": "2026-03-26T00:00:00Z",
  "open_interest_contracts": 10659.509,
  "mark_price": 71816.1,
  "open_interest_value_usdt": 765394630.0,
  "payload_json": {}
}
```

和：

```json
{
  "stream_name": "futures/data/openInterestHist",
  "ts_recv": "2026-03-26T00:00:00Z",
  "ts_effective": "2026-03-26T00:00:00Z",
  "ts_bucket": "2026-03-26T00:00:00Z",
  "period": "5m",
  "open_interest_contracts": 20403.637,
  "open_interest_value_usdt": 150570784.0781,
  "payload_json": {}
}
```

实现约束：

- `msg_type` 分别为：
  - `md.open_interest_current`
  - `md.open_interest_hist_5m`
- `routing_key` 分别为：
  - `md.futures.open_interest.current.{symbol_lower}`
  - `md.futures.open_interest.5m.{symbol_lower}`
- `event_ts = ts_effective`

### `/data/systems/market_data_ingestor/src/normalize/long_short_ratio_normalizer.rs`

建议入口：

```rust
pub fn normalize_long_short_ratio_rest(
    symbol: &str,
    ratio_type: LongShortRatioType,
    row: &BinanceLongShortRatioRecord,
    backfill_in_progress: bool,
) -> Result<NormalizedMdEvent>;
```

`data` 建议固定为：

```json
{
  "stream_name": "futures/data/globalLongShortAccountRatio",
  "ts_recv": "2026-03-26T00:00:00Z",
  "ts_effective": "2026-03-26T00:00:00Z",
  "ts_bucket": "2026-03-26T00:00:00Z",
  "period": "5m",
  "ratio_type": "global_account",
  "long_short_ratio": 1.9559,
  "long_account_ratio": 0.6617,
  "short_account_ratio": 0.3382,
  "payload_json": {}
}
```

实现约束：

- `msg_type = "md.long_short_ratio_5m"`
- `routing_key` 需按 ratio 类型区分：
  - `md.futures.long_short_ratio.global_account.5m.{symbol_lower}`
  - `md.futures.long_short_ratio.top_account.5m.{symbol_lower}`
  - `md.futures.long_short_ratio.top_position.5m.{symbol_lower}`

### `/data/systems/market_data_ingestor/src/normalize/mod.rs`

需要补两类内容：

- `pub mod open_interest_normalizer;`
- `pub mod long_short_ratio_normalizer;`

这里不需要修改 `normalize_ws_event(...)`，因为这次新增源全是 REST，不是 WS。

### `/data/systems/market_data_ingestor/src/state/backfill_scheduler.rs`

建议新增 4 组 job 入口：

```rust
pub async fn run_open_interest_current_loop(...);
pub async fn run_open_interest_hist_5m_loop(...);
pub async fn run_long_short_ratio_5m_loop(...);

pub async fn backfill_open_interest_hist_5m(...);
pub async fn backfill_long_short_ratio_5m(...);
```

关键行为约束：

- `current OI` 每 `1m` 拉一次。
- `OI hist / ratios` 每个 `5m` 闭合后再拉，避免半根桶。
- `5m` live loop 每次重拉最近 `2` 个 bucket，依赖 upsert 去重。
- startup backfill 的起点取 `DB max(ts_bucket) - 1 bucket overlap`。

### `/data/systems/market_data_ingestor/src/app/runtime.rs`

只负责挂起任务，不做业务计算。建议新增：

- `spawn_open_interest_current_loop(...)`
- `spawn_open_interest_hist_5m_loop(...)`
- `spawn_long_short_ratio_5m_loop(...)`

并在 heartbeat / shutdown 路径里把这三类任务纳入管理。

### `/data/systems/market_data_ingestor/src/sinks/md_db_writer.rs`

建议新增 3 个单条写入方法和 2 个批量写入方法：

```rust
async fn insert_open_interest_current_1m(&self, event: &NormalizedMdEvent) -> Result<()>;
async fn insert_open_interest_hist_5m(&self, event: &NormalizedMdEvent) -> Result<()>;
async fn insert_long_short_ratio_5m(&self, event: &NormalizedMdEvent) -> Result<()>;

async fn insert_open_interest_hist_5m_batch(
    &self,
    tx: &mut Transaction<'_, Postgres>,
    rows: &[OpenInterestHist5mBatchRow],
) -> Result<()>;

async fn insert_long_short_ratio_5m_batch(
    &self,
    tx: &mut Transaction<'_, Postgres>,
    rows: &[LongShortRatio5mBatchRow],
) -> Result<()>;
```

建议新增的 batch row：

```rust
struct OpenInterestHist5mBatchRow {
    ts_event: DateTime<Utc>,
    ts_bucket: DateTime<Utc>,
    market: String,
    symbol: String,
    source_kind: String,
    stream_name: String,
    open_interest_contracts: f64,
    open_interest_value_usdt: f64,
    payload_json: serde_json::Value,
}

struct LongShortRatio5mBatchRow {
    ts_event: DateTime<Utc>,
    ts_bucket: DateTime<Utc>,
    market: String,
    symbol: String,
    source_kind: String,
    stream_name: String,
    ratio_type: String,
    long_short_ratio: f64,
    long_account_ratio: f64,
    short_account_ratio: f64,
    payload_json: serde_json::Value,
}
```

### `/data/systems/market_data_ingestor/src/observability/metrics.rs`

建议新增这些指标名：

- `binance_rest_open_interest_current_ok_total`
- `binance_rest_open_interest_current_err_total`
- `binance_rest_open_interest_hist_ok_total`
- `binance_rest_open_interest_hist_err_total`
- `binance_rest_long_short_ratio_ok_total`
- `binance_rest_long_short_ratio_err_total`
- `md_open_interest_current_rows_written_total`
- `md_open_interest_hist_rows_written_total`
- `md_long_short_ratio_rows_written_total`

---

## 14.2 orderflow-indicator-engine

### `/data/systems/indicator_engine/src/ingest/decoder.rs`

建议新增 3 个 event struct：

```rust
pub struct OpenInterestCurrentEvent {
    pub ts_event: DateTime<Utc>,
    pub market: MarketKind,
    pub symbol: String,
    pub open_interest_contracts: f64,
    pub mark_price: Option<f64>,
    pub open_interest_value_usdt: Option<f64>,
    pub payload_json: serde_json::Value,
}

pub struct OpenInterestHist5mEvent {
    pub ts_event: DateTime<Utc>,
    pub ts_bucket: DateTime<Utc>,
    pub market: MarketKind,
    pub symbol: String,
    pub open_interest_contracts: f64,
    pub open_interest_value_usdt: f64,
    pub payload_json: serde_json::Value,
}

pub struct LongShortRatio5mEvent {
    pub ts_event: DateTime<Utc>,
    pub ts_bucket: DateTime<Utc>,
    pub market: MarketKind,
    pub symbol: String,
    pub ratio_type: String,
    pub long_short_ratio: f64,
    pub long_account_ratio: f64,
    pub short_account_ratio: f64,
    pub payload_json: serde_json::Value,
}
```

并扩展：

- `MdData`
- `EngineEvent`
- `decode_md_row(...)`

支持以下 `msg_type`：

- `md.open_interest_current`
- `md.open_interest_hist_5m`
- `md.long_short_ratio_5m`

说明：

- `5m` 对这两类历史事件是固定语义，不需要在 DB 单独存 `period_code` 列。
- 如上层 struct 或 snapshot payload 仍想保留 `period="5m"`，可在 decoder / indicator 输出阶段直接补常量。

### `/data/systems/indicator_engine/src/runtime/state_store.rs`

建议新增最小状态结构：

```rust
pub struct LatestOpenInterestState {
    pub ts: DateTime<Utc>,
    pub open_interest_contracts: f64,
    pub mark_price: Option<f64>,
    pub open_interest_value_usdt: Option<f64>,
}

pub struct OpenInterestPoint {
    pub ts_bucket: DateTime<Utc>,
    pub open_interest_contracts: f64,
    pub open_interest_value_usdt: f64,
}

pub struct LongShortRatioPoint {
    pub ts_bucket: DateTime<Utc>,
    pub ratio_type: String,
    pub long_short_ratio: f64,
    pub long_account_ratio: f64,
    pub short_account_ratio: f64,
}
```

建议在 `StateStore` 上新增这些字段：

- `latest_open_interest: Option<LatestOpenInterestState>`
- `open_interest_hist_5m: VecDeque<OpenInterestPoint>`
- `long_short_ratio_hist_5m: HashMap<String, VecDeque<LongShortRatioPoint>>`

建议新增这些方法：

```rust
pub fn ingest_open_interest_current(&mut self, event: OpenInterestCurrentEvent);
pub fn ingest_open_interest_hist_5m(&mut self, event: OpenInterestHist5mEvent);
pub fn ingest_long_short_ratio_5m(&mut self, event: LongShortRatio5mEvent);

pub fn open_interest_window(
    &self,
    ts_bucket: DateTime<Utc>,
    mins: i64,
) -> Vec<OpenInterestPoint>;

pub fn long_short_ratio_window(
    &self,
    ratio_type: &str,
    ts_bucket: DateTime<Utc>,
    mins: i64,
) -> Vec<LongShortRatioPoint>;
```

实现约束：

- `open_interest_hist_5m` 和 `long_short_ratio_hist_5m` 至少保留 `10d`，以覆盖 `3d` 窗口和 replay overlap。
- 按 `ts_bucket` 去重，重复 bucket 以“后到覆盖前到”为准。
- `latest_open_interest` 只保留最新 `1m current`，不需要额外维护长序列。

### `/data/systems/indicator_engine/src/indicators/i25_open_interest.rs`

这个文件只负责从 state 计算 `OI` 指标，不直接碰 DB。

建议输出字段：

```json
{
  "latest_effective_ts": "2026-03-26T12:10:00Z",
  "oi_last_contracts": 20403.637,
  "oi_last_value_usdt": 150570784.0781,
  "oi_change_contracts": -420.55,
  "oi_change_pct": -0.0202,
  "oi_change_value_usdt": -3210044.12,
  "oi_change_value_pct": -0.0211,
  "oi_range_low_contracts": 19888.11,
  "oi_range_high_contracts": 20999.32,
  "oi_slope_per_5m": -61.22,
  "oi_delta_zscore": -2.14,
  "price_change_pct": -0.0137,
  "oi_price_regime": "price_down_oi_down",
  "positioning_interpretation": "long_unwind"
}
```

建议规则：

- `oi_price_regime` 只使用这 5 个值：
  - `price_up_oi_up`
  - `price_up_oi_down`
  - `price_down_oi_up`
  - `price_down_oi_down`
  - `mixed`
- `positioning_interpretation` 只使用这 5 个值：
  - `new_longs`
  - `short_covering`
  - `new_shorts`
  - `long_unwind`
  - `mixed`

### `/data/systems/indicator_engine/src/indicators/i26_long_short_ratios.rs`

建议输出字段：

```json
{
  "latest_effective_ts": "2026-03-26T12:10:00Z",
  "global_account_ratio_last": 1.9559,
  "global_account_long_pct_last": 0.6617,
  "global_account_short_pct_last": 0.3382,
  "global_account_ratio_change": 0.118,
  "top_account_ratio_last": 2.1142,
  "top_account_long_pct_last": 0.6789,
  "top_account_short_pct_last": 0.3211,
  "top_account_ratio_change": 0.203,
  "top_position_ratio_last": 2.4874,
  "top_position_long_pct_last": 0.7131,
  "top_position_short_pct_last": 0.2869,
  "top_position_ratio_change": 0.261,
  "top_vs_global_account_spread": 0.1583,
  "top_position_vs_top_account_spread": 0.3732,
  "crowding_side": "long",
  "crowding_strength": "elevated"
}
```

建议规则：

- `crowding_side ∈ {"long","short","balanced"}`
- `crowding_strength ∈ {"neutral","elevated","extreme"}`
- `top_vs_global_account_spread = top_account_ratio_last - global_account_ratio_last`
- `top_position_vs_top_account_spread = top_position_ratio_last - top_account_ratio_last`

### `/data/systems/indicator_engine/src/storage/feature_writer.rs`

建议新增 2 个写库方法：

```rust
async fn insert_open_interest_feature_window(
    &self,
    ctx: &IndicatorContext,
    window_code: &str,
    mins: i64,
) -> Result<()>;

async fn insert_long_short_ratio_feature_window(
    &self,
    ctx: &IndicatorContext,
    window_code: &str,
    mins: i64,
) -> Result<()>;
```

设计约束：

- 这两个方法只对以下窗口生效：
  - `5m`
  - `15m`
  - `4h`
  - `1d`
  - `3d`
- 全局 `write_all(...)` 仍可能带有 `1m` 默认窗口，但这里要主动跳过 `1m`。
- `extra_json` 里建议保留：
  - `sample_count`
  - `window_minutes`
  - `first_bucket_ts`
  - `last_bucket_ts`
  - `component_zscores`

### `/data/systems/indicator_engine/src/app/bootstrap.rs`

这里只做窗口合法性改造：

- `default_window_codes()` 补 `5m`
- 配置校验允许 `5m`

不要求所有老指标都支持 `5m`，只是让 `i25 / i26` 能合法注册和落库。

### `/data/systems/indicator_engine/src/app/runtime.rs`

需要明确 3 类改动：

- replay SQL union 新 `md.*`
- `INDICATOR_COVERAGE_ORDER` 从 24 扩到 26
- catalog 注册：
  - `("i25", "open_interest")`
  - `("i26", "long_short_ratios")`

### `/data/systems/indicator_engine/src/indicators/mod.rs`

补：

```rust
pub mod i25_open_interest;
pub mod i26_long_short_ratios;
```

### `/data/systems/indicator_engine/src/indicators/registry.rs`

补：

- `Box::new(I25OpenInterest::default())`
- `Box::new(I26LongShortRatios::default())`

并确保：

- `indicator_code = "open_interest"`
- `indicator_code = "long_short_ratios"`

与文档和 `cfg.indicator_catalog` 一致。

---

## 15. SQL 草案骨架

## 15.1 设计原则

- 新增 `md.*` 表建议保持为普通表，理由和现有 `md.agg_*` 一致：
  - 写入量低
  - 需要稳定 natural upsert key
  - 不值得为低频 REST 数据引入 hypertable 唯一键限制
- 新增 `feat.*` 表继续使用 hypertable，和现有 `feat.trade_flow_feature / feat.funding_feature` 风格保持一致。
- 所有新表都沿用：
  - `venue`
  - `market`
  - `symbol`
  - `source_kind`
  - `payload_json / extra_json`
- 对 `_5m` 规范表，不再重复存 `period_code='5m'` 这类冗余列，避免增大行宽和唯一索引宽度。
- 除幂等唯一键外，只保留与真实查询路径匹配的一条次级 lookup 索引，不预先堆叠多条“可能会用到”的索引。

## 15.2 `20260326_md_open_interest_current_1m.sql`

```sql
CREATE TABLE IF NOT EXISTS md.open_interest_current_1m (
    ts_event                 TIMESTAMPTZ NOT NULL,
    ts_recv                  TIMESTAMPTZ NOT NULL DEFAULT now(),
    venue                    TEXT NOT NULL DEFAULT 'binance',
    market                   cfg.market_type NOT NULL,
    symbol                   TEXT NOT NULL,
    source_kind              cfg.source_type NOT NULL DEFAULT 'rest',
    stream_name              TEXT NOT NULL DEFAULT 'fapi/v1/openInterest',
    open_interest_contracts  DOUBLE PRECISION NOT NULL,
    mark_price               DOUBLE PRECISION,
    open_interest_value_usdt DOUBLE PRECISION,
    payload_json             JSONB NOT NULL DEFAULT '{}'::jsonb,
    CHECK (open_interest_contracts >= 0),
    CHECK (open_interest_value_usdt IS NULL OR open_interest_value_usdt >= 0)
);

CREATE UNIQUE INDEX IF NOT EXISTS uq_open_interest_current_1m_natural
ON md.open_interest_current_1m(market, symbol, ts_event);

CREATE INDEX IF NOT EXISTS idx_open_interest_current_1m_lookup
ON md.open_interest_current_1m(symbol, ts_event DESC);
```

## 15.3 `20260326_md_open_interest_hist_5m.sql`

```sql
CREATE TABLE IF NOT EXISTS md.open_interest_hist_5m (
    ts_event                 TIMESTAMPTZ NOT NULL,
    ts_recv                  TIMESTAMPTZ NOT NULL DEFAULT now(),
    venue                    TEXT NOT NULL DEFAULT 'binance',
    market                   cfg.market_type NOT NULL,
    symbol                   TEXT NOT NULL,
    source_kind              cfg.source_type NOT NULL DEFAULT 'rest',
    stream_name              TEXT NOT NULL DEFAULT 'futures/data/openInterestHist',
    ts_bucket                TIMESTAMPTZ NOT NULL,
    open_interest_contracts  DOUBLE PRECISION NOT NULL,
    open_interest_value_usdt DOUBLE PRECISION NOT NULL,
    payload_json             JSONB NOT NULL DEFAULT '{}'::jsonb,
    CHECK (open_interest_contracts >= 0),
    CHECK (open_interest_value_usdt >= 0)
);

CREATE UNIQUE INDEX IF NOT EXISTS uq_open_interest_hist_5m_natural
ON md.open_interest_hist_5m(market, symbol, ts_bucket);

CREATE INDEX IF NOT EXISTS idx_open_interest_hist_5m_lookup
ON md.open_interest_hist_5m(symbol, ts_bucket DESC);
```

## 15.4 `20260326_md_long_short_ratio_5m.sql`

```sql
CREATE TABLE IF NOT EXISTS md.long_short_ratio_5m (
    ts_event             TIMESTAMPTZ NOT NULL,
    ts_recv              TIMESTAMPTZ NOT NULL DEFAULT now(),
    venue                TEXT NOT NULL DEFAULT 'binance',
    market               cfg.market_type NOT NULL,
    symbol               TEXT NOT NULL,
    source_kind          cfg.source_type NOT NULL DEFAULT 'rest',
    stream_name          TEXT NOT NULL,
    ratio_type           TEXT NOT NULL,
    ts_bucket            TIMESTAMPTZ NOT NULL,
    long_short_ratio     DOUBLE PRECISION NOT NULL,
    long_account_ratio   DOUBLE PRECISION NOT NULL,
    short_account_ratio  DOUBLE PRECISION NOT NULL,
    payload_json         JSONB NOT NULL DEFAULT '{}'::jsonb,
    CHECK (ratio_type IN ('global_account', 'top_account', 'top_position'))
);

CREATE UNIQUE INDEX IF NOT EXISTS uq_long_short_ratio_5m_natural
ON md.long_short_ratio_5m(market, symbol, ratio_type, ts_bucket);

CREATE INDEX IF NOT EXISTS idx_long_short_ratio_5m_lookup
ON md.long_short_ratio_5m(symbol, ratio_type, ts_bucket DESC);
```

## 15.5 `20260326_feat_open_interest_feature.sql`

```sql
CREATE TABLE IF NOT EXISTS feat.open_interest_feature (
    ts_bucket                TIMESTAMPTZ NOT NULL,
    bar_interval             INTERVAL NOT NULL,
    venue                    TEXT NOT NULL DEFAULT 'binance',
    symbol                   TEXT NOT NULL,
    latest_effective_ts      TIMESTAMPTZ,
    oi_last_contracts        DOUBLE PRECISION,
    oi_last_value_usdt       DOUBLE PRECISION,
    oi_change_contracts      DOUBLE PRECISION,
    oi_change_pct            DOUBLE PRECISION,
    oi_change_value_usdt     DOUBLE PRECISION,
    oi_change_value_pct      DOUBLE PRECISION,
    oi_range_low_contracts   DOUBLE PRECISION,
    oi_range_high_contracts  DOUBLE PRECISION,
    oi_slope_per_5m          DOUBLE PRECISION,
    oi_delta_zscore          DOUBLE PRECISION,
    price_change_pct         DOUBLE PRECISION,
    oi_price_regime          TEXT,
    positioning_interpretation TEXT,
    extra_json               JSONB NOT NULL DEFAULT '{}'::jsonb,
    param_set_id             UUID,
    calc_version             TEXT,
    created_at               TIMESTAMPTZ NOT NULL DEFAULT now(),
    CHECK (
        oi_price_regime IS NULL OR
        oi_price_regime IN (
            'price_up_oi_up',
            'price_up_oi_down',
            'price_down_oi_up',
            'price_down_oi_down',
            'mixed'
        )
    ),
    CHECK (
        positioning_interpretation IS NULL OR
        positioning_interpretation IN (
            'new_longs',
            'short_covering',
            'new_shorts',
            'long_unwind',
            'mixed'
        )
    )
);

SELECT create_hypertable('feat.open_interest_feature', 'ts_bucket', if_not_exists => TRUE);

CREATE UNIQUE INDEX IF NOT EXISTS uq_open_interest_feature_natural
ON feat.open_interest_feature(venue, symbol, bar_interval, ts_bucket);

CREATE INDEX IF NOT EXISTS idx_open_interest_feature_lookup
ON feat.open_interest_feature(symbol, bar_interval, ts_bucket DESC);
```

## 15.6 `20260326_feat_long_short_ratio_feature.sql`

```sql
CREATE TABLE IF NOT EXISTS feat.long_short_ratio_feature (
    ts_bucket                          TIMESTAMPTZ NOT NULL,
    bar_interval                       INTERVAL NOT NULL,
    venue                              TEXT NOT NULL DEFAULT 'binance',
    symbol                             TEXT NOT NULL,
    latest_effective_ts                TIMESTAMPTZ,
    global_account_ratio_last          DOUBLE PRECISION,
    global_account_long_pct_last       DOUBLE PRECISION,
    global_account_short_pct_last      DOUBLE PRECISION,
    global_account_ratio_change        DOUBLE PRECISION,
    top_account_ratio_last             DOUBLE PRECISION,
    top_account_long_pct_last          DOUBLE PRECISION,
    top_account_short_pct_last         DOUBLE PRECISION,
    top_account_ratio_change           DOUBLE PRECISION,
    top_position_ratio_last            DOUBLE PRECISION,
    top_position_long_pct_last         DOUBLE PRECISION,
    top_position_short_pct_last        DOUBLE PRECISION,
    top_position_ratio_change          DOUBLE PRECISION,
    top_vs_global_account_spread       DOUBLE PRECISION,
    top_position_vs_top_account_spread DOUBLE PRECISION,
    crowding_side                      TEXT,
    crowding_strength                  TEXT,
    extra_json                         JSONB NOT NULL DEFAULT '{}'::jsonb,
    param_set_id                       UUID,
    calc_version                       TEXT,
    created_at                         TIMESTAMPTZ NOT NULL DEFAULT now(),
    CHECK (crowding_side IS NULL OR crowding_side IN ('long', 'short', 'balanced')),
    CHECK (crowding_strength IS NULL OR crowding_strength IN ('neutral', 'elevated', 'extreme'))
);

SELECT create_hypertable('feat.long_short_ratio_feature', 'ts_bucket', if_not_exists => TRUE);

CREATE UNIQUE INDEX IF NOT EXISTS uq_long_short_ratio_feature_natural
ON feat.long_short_ratio_feature(venue, symbol, bar_interval, ts_bucket);

CREATE INDEX IF NOT EXISTS idx_long_short_ratio_feature_lookup
ON feat.long_short_ratio_feature(symbol, bar_interval, ts_bucket DESC);
```

## 15.7 `20260326_indicator_catalog_oi_ratio.sql`

```sql
INSERT INTO cfg.indicator_catalog (
    indicator_code,
    display_name,
    category,
    output_kind,
    primary_market,
    needs_spot_confirm,
    notes
)
VALUES
    (
        'open_interest',
        'Open Interest',
        'risk',
        'pack',
        'futures',
        FALSE,
        'Derived from Binance futures open interest current + 5m history.'
    ),
    (
        'long_short_ratios',
        'Long Short Ratios',
        'risk',
        'pack',
        'futures',
        FALSE,
        'Derived from Binance global/top trader long-short ratio endpoints.'
    )
ON CONFLICT (indicator_code) DO UPDATE SET
    display_name = EXCLUDED.display_name,
    category = EXCLUDED.category,
    output_kind = EXCLUDED.output_kind,
    primary_market = EXCLUDED.primary_market,
    needs_spot_confirm = EXCLUDED.needs_spot_confirm,
    notes = EXCLUDED.notes;
```

## 15.8 可选：`cfg.rest_job_catalog` seed 草案

如果运行时确实使用 `cfg.rest_job_catalog` 管理 REST 作业，可以再补一份 seed：

```sql
INSERT INTO cfg.rest_job_catalog (
    venue,
    symbol,
    market,
    endpoint_path,
    purpose,
    schedule_mode,
    schedule_expr,
    enabled
)
VALUES
    ('binance', 'BTCUSDT', 'futures', '/fapi/v1/openInterest', 'open_interest_current_1m', 'interval', '1m', TRUE),
    ('binance', 'BTCUSDT', 'futures', '/futures/data/openInterestHist', 'open_interest_hist_5m', 'interval', '5m', TRUE),
    ('binance', 'BTCUSDT', 'futures', '/futures/data/globalLongShortAccountRatio', 'global_long_short_ratio_5m', 'interval', '5m', TRUE),
    ('binance', 'BTCUSDT', 'futures', '/futures/data/topLongShortAccountRatio', 'top_long_short_account_ratio_5m', 'interval', '5m', TRUE),
    ('binance', 'BTCUSDT', 'futures', '/futures/data/topLongShortPositionRatio', 'top_long_short_position_ratio_5m', 'interval', '5m', TRUE)
ON CONFLICT DO NOTHING;
```

这部分不是硬要求，如果调度仍由 `config.yaml + runtime` 管，也可以不落。

## 16. 结论

这次改造的本质不是“再加两个指标”，而是给当前系统补上**仓位结构数据源**。

真正需要落地的是三件事：

- `market_data_ingestor` 把 Binance 的 `OI` 和 `Long-Short Ratios` 拉进来，并写入 `md.*`
- 数据库新增 `md.*` 和 `feat.*` 表，保证可回放、可诊断、可重算
- `indicator_engine` 把这批 `5m` 规范序列聚合成 `5m / 15m / 4h / 1d / 3d`，并写入 `temp_indicator`

只要这三层按本文设计打通，上层以后怎么用这些字段，是下一阶段的事；本次任务到此为止。
